
&НаСервереБезКонтекста
Процедура СписокПриПолученииДанныхНаСервере ( ИмяЭлемента, Настройки, Строки )
	
	ключи = Строки.ПолучитьКлючи ();
	м = Новый Массив ();
	Для Каждого ключ Из Ключи Цикл
		м.Добавить ( ключ.Ссылка );		
	КонецЦикла;
	адреса = Серверный.ПолучитьАдресПодключенияКонтрагента ( м );
	Для Каждого адрес Из адреса Цикл
		строкаСписка = Строки [ адрес.Ключ ];
		строкаСписка.Данные.АдресПодключенияВеб = адрес.Значение;
	КонецЦикла;
	
КонецПроцедуры